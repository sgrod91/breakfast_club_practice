//Given an array of words, write a function vowelCounts that, given an array of
//words, returns an array of vowel counts of the words in the array.
//Use the array.prototype.map in the solution

var people = [
  'Dom',
  'Lyn',
  'Kirk',
  'Autumn',
  'Trista',
  'Jesslyn',
  'Ben'
];

function vowelCounts(string) {
  for (var i = 0; i <= string.length - 1; i++);
  if (string.charAt(i) == "a" || string.charAt(i) == "e" || string.charAt(i) == "i" || string.charAt(i) == "o" || string.charAt(i) == "u") {
  vowelsCount += 1;
}
}
